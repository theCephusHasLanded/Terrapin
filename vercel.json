{
  "buildCommand": "npm run build || exit 0",
  "ignoreCommand": "echo 'Ignoring TypeScript errors for now'",
  "installCommand": "npm install",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "skipBuildCommand": true
      }
    }
  ]
}